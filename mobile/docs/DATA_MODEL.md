# Model Data

`trees` memiliki relasi ke `gardens`. `observation_sessions` menjadi induk observasi visual dan pengukuran. Raw observations, events, predictions, corrections, labels, serta model metadata disimpan terpisah dan dapat diaudit.
